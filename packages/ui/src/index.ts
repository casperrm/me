// Shared UI system (Bible Section 35). apps/web owns its own components
// (src/components) for now — there is only one web app, so extracting a
// shared component library would be speculative. Move a component here
// the moment a second UI surface (e.g. the Section 15.2 Client Portal, if
// it ends up as its own app) needs to render the same thing.
export {};
