# infra

Deployment/infrastructure definitions (Bible Section 35).

Empty until ADR-010 (deployment platform) is decided — see
`docs/adr/0010-deployment-platform-and-observability.md`. Nothing in this
repo depends on this directory yet; local development runs against a
locally installed PostgreSQL + Redis (see the root `README.md`).
